<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripproduct %><br>
    <%= f.text_field :descripproduct %>
  </div>
  <div class="field">
    <%= f.label :priceunit %><br>
    <%= f.text_field :priceunit %>
  </div>
  
  <div class="field">
    <%= f.label :category %><br>
    <%= f.select("category_id", Category.all.collect {|p| [ p.descripcategory, p.id ] }, {include_blank: 'None'}) %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
